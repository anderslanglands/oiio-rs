# Building
Tested on OSX only. Must have Boost (1.61 or compatible) installed already. Can specify path to Boost using BOOST_ROOT environment variable, as in:
```bash
env BOOST_ROOT=/path/to/boost cargo build
```